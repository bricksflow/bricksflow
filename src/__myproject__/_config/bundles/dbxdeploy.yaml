parameters:
  dbxdeploy:
    source:
      notebooks:
        baseDir: 'src/__myproject__'
    target:
      workspace:
        baseDir: '%databricks.workspace.baseDir%'
    databricks:
      host: '%databricks.address%'
      token: '%env(DBX_TOKEN)%'
      clusterId: '%databricks.clusterId%'
